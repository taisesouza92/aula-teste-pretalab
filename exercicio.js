// 2. Criar 5 variáveis utilizando o padrão camelCase, e adicionar dados à elas. Pode ser string, number int, number float ou null.

// 3. Criar duas variáveis, atribuir os valores booleanos true e false, e utilizar os operadores lógicos para testar seus resultados no console.

let frutaFavorita = "banana";
let numeroTelefone = 98788788;
const descontoFuncionario= 0.10; 
let pegarMetro= true;
let capitalBrasileira='Salvador';

let pegarTrem= true;
let transportePublico=true;
let viajarFerias= false;

